# HitGS

Simple web application intended to test WFS and WMS requests on GeoServer.

Still under development, so it might not work at all...

Currently uses a reverse proxy technique to bypass the requests sent to GeoServer. This is necessary in order to avoid CORS.